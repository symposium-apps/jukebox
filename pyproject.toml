[project]
name = "player"
version = "0.1.1"
description = "SYM music player with an app-scoped library and browser controls."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["player"]
