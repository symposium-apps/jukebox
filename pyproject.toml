[project]
name = "player"
version = "0.1.0"
description = "Wearable Raspberry Pi MP3 player and suit control module."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["player"]
